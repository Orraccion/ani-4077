La sortie "jenga info"
    No .jenga workspace file found.

La sortie "jenga build"
    No .jenga workspace file found.


Lorsque je commente le workspace le fichier jenga s'exécute normalement mais ne trouve pas le workspace, du coup le compilateur dit qu'il n'y a pas de fichier jenga (pourtant présent) alors que son véritable problème c'est le workspace qui n'est pas trouvé
